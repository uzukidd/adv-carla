trainer:
  num_sanity_val_steps: 0
  max_epochs: 1
  use_distributed_sampler: false

data:
  pcdet_dataset_config: 
    _BASE_CONFIG_: cfgs/pcdet/dataset_configs/kitti_carla_dataset.yaml
    DATA_PATH: data/kitti_carla

    MIN_POINTS_IN_ALL_ANNO: 10 

    DATA_SPLIT: {
        'train': null,
        'test': train
    }

    INFO_PATH: {
        'train': [null],
        'test': [kitti_infos_train.pkl],
    }

    POINT_CLOUD_RANGE: [0, -39.68, -3, 69.12, 39.68, 1]
    DATA_PROCESSOR:
        - NAME: mask_points_and_boxes_outside_range
          REMOVE_OUTSIDE_BOXES: True

        - NAME: shuffle_points
          SHUFFLE_ENABLED: {
            'train': True,
            'test': False
          }

        - NAME: collate_gtboxes
          target_label: [1]
        
        - NAME: physical_adversary

        - NAME: constant_reflectness
          constant: 0.0

        - NAME: differentiable_voxelize
          RETAIN_GRAPH: False
          END_TO_END_MODE: True
          DETERMINISTIC: True
          VOXEL_SIZE: [0.16, 0.16, 4]
          MAX_POINTS_PER_VOXEL: 32
          MAX_NUMBER_OF_VOXELS: {
            'train': 40000,
            'test': 40000
                  }

model:
  adversary_config:
    benchmark_path: assets/benchmark/kitti_carla/pp/clean.json
    car_adv_patch_scale: [0.7, 0.7, 0.5]
    car_adv_patch_level: 2
  target_encoder: 
    class_path: attack_utils.end_to_end_detector
  pcdet_model_config: 
    _BASE_CONFIG_: cfgs/pcdet/kitti_carla_models/pointpillar.yaml
    ckpt_path: modelzoo/pcdet/kitti_carla/pointpillar.pth