
ATTACK_CONFIG: 
    ROOFTOP_ANNOTATE: "data/rooftop_approximation/rooftop_appro_kitti.pkl"
    DETECTOR: "single"
    TARGET_COMPONENT: null
    TRAINING_COMPONENTS: []

SURROGATE_ATTACK_CONFIG: 
    ROOFTOP_ANNOTATE: "data/rooftop_approximation/rooftop_appro_kitti.pkl"
    DETECTOR: "rcnn"
    TARGET_COMPONENT: "PointHeadBox"
    TRAINING_COMPONENTS: []

DATA_CONFIG:
    _BASE_CONFIG_: configs/dataset_configs/kitti_dataset.yaml

    DATA_PROCESSOR:
        - NAME: mask_points_and_boxes_outside_range
          REMOVE_OUTSIDE_BOXES: True

        - NAME: shuffle_points
          SHUFFLE_ENABLED: {
            'train': True,
            'test': False
          }

        - NAME: transform_points_to_voxels_placeholder
          VOXEL_SIZE: [0.05, 0.05, 0.1]
          MAX_POINTS_PER_VOXEL: 5
          MAX_NUMBER_OF_VOXELS: {
            'train': 16000,
            'test': 40000
          }
    
    ADVANCED_DATA_PROCESSOR: 
        - NAME: remove_reflective
        - NAME: differentiable_voxelize
          VOXEL_SIZE: [0.05, 0.05, 0.1]
          MAX_POINTS_PER_VOXEL: 5
          MAX_NUMBER_OF_VOXELS: {
            'train': 16000,
            'test': 40000
          }

SURROGATE_DATA_CONFIG:
    _BASE_CONFIG_: configs/dataset_configs/kitti_dataset.yaml

    DATA_PROCESSOR:
        -   NAME: mask_points_and_boxes_outside_range
            REMOVE_OUTSIDE_BOXES: True

        -   NAME: shuffle_points
            SHUFFLE_ENABLED: {
                'train': True,
                'test': False
            }
    
    ADVANCED_DATA_PROCESSOR: 
        -   NAME: remove_reflective
        -   NAME: sample_points_gpu
            NUM_POINTS: {
                'train': 16384,
                'test': 16384
            }



MODEL:
    CLASS_NAMES: ['Car', 'Pedestrian', 'Cyclist']
    _BASE_CONFIG_: configs/model_configs/second.yaml
    CKPT_PATH: "/home/ksas/Public/model_zoo/pcdet/second_7862.pth"
SURROGATE_MODEL:
    CLASS_NAMES: ['Car', 'Pedestrian', 'Cyclist']
    _BASE_CONFIG_: configs/model_configs/pointrcnn.yaml
    CKPT_PATH: "/home/ksas/Public/model_zoo/pcdet/pointrcnn_7870.pth"